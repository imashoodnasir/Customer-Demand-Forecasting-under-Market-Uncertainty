# Bayesian Retail Forecasting - Final Reproducibility Package

This package provides the final project organization layer.

Workflow:

1. Install environment
2. Prepare datasets
3. Generate features
4. Train models
5. Evaluate results
6. Generate tables and figures

Main command:

python scripts/reproduce_paper.py --mode full
